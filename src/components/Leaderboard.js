import React, {Component} from 'react';

export default class Leaderboard extends Component {
  render () {
    const {pact} = this.props;

    const styles = {};

    return (
      <div>
      </div>
    );
  }
}
